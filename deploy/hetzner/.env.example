# OpenClaw Hetzner Secure Deployment — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it's in .gitignore.

# === Required ===
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest
OPENCLAW_GATEWAY_TOKEN=        # Your gateway token (auto-generated during setup, or set one)
OPENCLAW_GATEWAY_BIND=127.0.0.1
OPENCLAW_GATEWAY_PORT=18789

# === Optional (set if using these integrations) ===
# TAILSCALE_AUTH_KEY=           # Tailscale auth key for secure remote access
# DISCORD_TOKEN=                # Discord bot token
# TELEGRAM_BOT_TOKEN=           # Telegram bot token
# BRAVE_API_KEY=                # Brave Search API key
# OPENAI_API_KEY=               # OpenAI API key
# ANTHROPIC_API_KEY=            # Anthropic API key

# === Auto-generated during setup (don't change unless needed) ===
# GOG_KEYRING_PASSWORD=         # Auto-generated during setup
# XDG_CONFIG_HOME=/home/node/.openclaw
